# CONES Developers Meeting
Date: 02/06/2023
People: Paolo, Miguel, Tom
Reporter: Tom

## Context

This has been the second of several meetings (that will be held on a weekly basis) to discuss on current and future developments that have to be done in CONES.

## Current topics and actions:

### Work done during the week

Lucas updated the decision tree, and it is on the GitHub, on the `interface` branch.
Some boxes must be added to complete some choices in the parameters.

Lucas opened a GitHub issue about things not working correctly. Some variables do not accept what the user gives, and thus the default value is put instead, for instance. 

Paolo suggests to re-write the way that the questions are asked on the Python file, and then retry to build a `cwipiConfig` file. 

One idea could be to divide the questions into sections, so the user has a clearer idea of what he is currently doing. Maybe gather questions by their interest (parameters, hyperparameters, ...). 

`hdf5` could be a nice format to save data about OpenFOAM, but does every OpenFOAM versions can handle `hdf5` extensions ?

#### Other talking
CONES not working on OpenFOAM v2206, issues in some OpenFOAM file differences between different versions. One solution could be to modify OpenFOAM files and recompile them to make CONES work. 

