# CONES Developers Meeting
Date: 23/06/2023
People: Paolo, Miguel, Tom
Reporter: Tom

## Context

This has been the second of several meetings (that will be held on a weekly basis) to discuss on current and future developments that have to be done in CONES.

## Current topics and actions:

### Work done during the week

Miguel worked about CONES parallelization.
The `triangulateCells` function has been put out of the `if` condition.

**!! Use `Foam::Pout` instead of `Foam::info` to show information about the parallelization. !!**

Also use `printf("%s: text_to_print", __func__);` to know which function sends the message. Useful to debug scripts. The goal is to make distinction between OpenFOAM and the KF. 

Sending is OK, but the return from the KF to OF is not working: the coupling couldn't be found. Therefore, the case is very specific for the `cavity` case, it must be adapted if used with other cases. 



#### Other talking

Talking about CWIPI, its pros and cons. Comparison with usage of MPIRUN environment. 
